{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "apresai-skills",
  "version": "1.11.0",
  "description": "Production-ready skills and plugins for Claude Code by apresai",
  "owner": {
    "name": "apresai",
    "email": "contact@apresai.com"
  },
  "plugins": [
    {
      "name": "apple-release",
      "description": "Apple app release automation for TestFlight and App Store Connect. Includes /release, /release-testflight, /release-consistency, /release-xcodecloud, and /app-store-audit (pre-submission risk audit against the full App Store Review Guidelines: project/binary AND App Store Connect metadata). Supports iOS, macOS, watchOS, visionOS.",
      "version": "1.6.7",
      "author": {
        "name": "apresai"
      },
      "source": "./plugins/apple-release",
      "category": "apple-development"
    },
    {
      "name": "go-lambda",
      "description": "Comprehensive Go Lambda development with provided.al2023 runtime, ARM64/Graviton2 architecture, AWS SDK v2, and CDK infrastructure. Covers scaffolding, migration, best practices, and production patterns.",
      "version": "1.5.1",
      "author": {
        "name": "apresai"
      },
      "source": "./plugins/go-lambda",
      "category": "aws-serverless"
    },
    {
      "name": "nextjs-opennext",
      "description": "Deploy Next.js applications to AWS Lambda using OpenNext and CDK. Covers App Router, Server Components, ISR, streaming, middleware, and production deployment patterns.",
      "version": "1.5.1",
      "author": {
        "name": "apresai"
      },
      "source": "./plugins/nextjs-opennext",
      "category": "aws-serverless"
    },
    {
      "name": "bubbletea-design",
      "description": "Build beautiful, idiomatic terminal UIs in Go with Bubble Tea v2, Bubbles v2, and Lip Gloss v2. Architecture patterns, component catalog, styling recipes, async/streaming patterns, v1→v2 migration map, and visual design principles for sexy TUIs.",
      "version": "1.5.1",
      "author": {
        "name": "Chad Neal"
      },
      "source": "./plugins/bubbletea-design",
      "category": "go-tui"
    },
    {
      "name": "image-encoding",
      "description": "Modern web image encoding with AVIF (primary) and WebP (fallback). Covers cwebp/dwebp/avifenc CLIs, the <picture> cascade pattern, browser-support cliffs, and when each format wins.",
      "version": "1.5.1",
      "author": {
        "name": "Chad Neal"
      },
      "source": "./plugins/image-encoding",
      "category": "image-processing"
    },
    {
      "name": "chad-review",
      "description": "6-pass autonomous pre-commit code review: drift, behavior-and-risk, tests, observability, dependency-freshness, and simplification, over uncommitted working-tree changes or the last commit if the tree is clean. Runs the project's own deterministic gate before any agent launches, and a repo with no validation entrypoint is itself a finding with a ready-to-paste target. Dependency freshness is a script rather than a prompt: it finds manifests, lockfiles, pinned versions, version-bearing references in files that are not manifests, and undeclared prerequisites, so doc, prompt, and IaC repos get audited instead of marked N/A. It cross-checks what the vulnerability scanner actually read against what it discovered, so an ecosystem nothing scanned is reported as a coverage gap rather than passing for clean, and it scans lockfiles that .gitignore hides from the scanner's own walk. Findings are grouped by the upgrade that closes them, so one bump fixing nine advisories reads as one decision. The pass updates dependencies rather than recommending updates: each ecosystem's own in-range command (npm update, go get -u, which never cross a major), kept only when the project's gate re-runs green, reverted otherwise, and left uncommitted for a separate deps: commit; what remains is judged, not queued: a trivial mechanical fix is taken, real breaking surface becomes a data-backed decision prompt, and bundled or transitive lag inside well-known libraries (an aws-cdk-lib bundle, the AWS SDK internals) warns without ever blocking. Diff-shape aware, one reviewer per language block, session-relative sonnet/opus tiering with an explicit model on every launch. NOT read-only: it never edits the change under review and never commits, but it updates dependency manifests and lockfiles as just described, and it executes your gate, tests, codegen, linters, and vulnerability scanner, all of which write to disk, and it backs up untracked files first because the no-edit rule is prompt-enforced rather than tool-enforced. Documentation drift is deterministic too: docs-drift.sh scans the complete contents of changed docs for lifecycle contradictions (a historical banner over a still-Active status line, or an index that classifies a doc one way while the doc says the other) and for stale operational values checked against authoritative sources (BUILD_NUMBER/VERSION files, manifests, pbxproj), preserving clearly labeled historical snapshots; contract-mirror.sh finds handwritten twins of definitions the generated contract now covers, plus the stale predates-style comments that keep them alive; and status-bearing docs get their own reviewer instead of riding the no-reviewer prose path, while trivial typo diffs stay light. Every completed review also emits a machine-readable receipt (verdict plus a stable diff fingerprint) that receipt.sh verify checks at the merge gate: the exact reviewed head passes, a clean rebase with an unchanged diff converges, and any substantive diff change, failed verdict, missing receipt, or generic look-alike review fails closed. Also ships /ultra-audit, a routed audit (leaf, deps, small, standard, audit) and the default pre-merge review; either tool's receipt satisfies receipt.sh verify at the merge gate.",
      "version": "2.8.2",
      "author": {
        "name": "Chad Neal"
      },
      "source": "./plugins/chad-review",
      "category": "code-quality"
    },
    {
      "name": "codex-br",
      "description": "Run OpenAI Codex on Amazon Bedrock (model openai.gpt-5.6-sol, provider amazon-bedrock) instead of the ChatGPT backend, via a `br` Codex profile. Adds /codex-br task (delegate a coding/diagnosis/research task), /codex-br review (built-in reviewer), and /codex-br adversarial-review (steerable challenge review). Defaults to maximum reasoning effort (ultra).",
      "version": "1.7.0",
      "author": {
        "name": "Chad Neal"
      },
      "source": "./plugins/codex-br",
      "category": "ai-tooling"
    },
    {
      "name": "xai-voice",
      "description": "Master reference for xAI's three voice APIs: Grok Text-to-Speech (POST /v1/tts), Speech-to-Text (/v1/stt), and the realtime Speech-to-Speech voice agent (wss://api.x.ai/v1/realtime). Live-verified request and response contracts, the full 26-voice catalog (docs still say 5), speech-tag semantics, a production Go client, and the doc-vs-reality discrepancies that ship broken audio.",
      "version": "1.6.2",
      "author": {
        "name": "Chad Neal"
      },
      "source": "./plugins/xai-voice",
      "category": "ai-tooling"
    }
  ]
}
